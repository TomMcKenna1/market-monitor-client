class Device {
    constructor(home, room, type, connected, uuid) {
        this.home = home;
        this.room = room;
        this.type = type;
        this.connected = connected;
        this.uuid = uuid;
    }
}

export default Device