class Device {
    constructor(name, type, connected, uuid) {
        this.name = name;
        this.type = type;
        this.connected = connected;
        this.uuid = uuid;
    }
}

export default Device